/// A class that defines [route] constants for different [screens] in the [application].
class AppRoutes {
  static const String splashScreen = '/'; // splash screen route
  static const String dashboardScreen = '/dashboard'; // dashboard screen route
}
